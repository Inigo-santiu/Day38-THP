// let read = .... "something something";
// read.addEventListener('click', function() {
//   classList.remove("le class ou attribute que nous allons supprimer pour avoir true ou false dans le truc...")
// });

// // options pour ligne 3 sont ====> classList.remove || setAttribute || removeAttribute



document.getElementById("object_mail").innerHTML = "<%= j render 'bodymail', email: @email %>";
document.getElementById("beta").innerHTML = "<%= j render 'readermail', email: @email %>";